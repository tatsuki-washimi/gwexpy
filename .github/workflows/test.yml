name: Tests

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # Install only runtime deps needed by tests, then install local package without resolving heavy optional deps.
          pip install "numpy<2.0" "scipy<1.14" "astropy<7.0" pandas matplotlib h5py "gwpy>=4.0.0,<5.0.0"
          pip install "scikit-learn"
          pip install -e . --no-deps
          pip install ruff pytest pytest-cov nbmake pytest-forked

      - name: Install GUI dependencies (conditional)
        if: contains(github.event.head_commit.message, '[gui-test]')
        run: |
          pip install pytest-qt qtpy PyQt5 pyqtgraph
          sudo apt-get update
          sudo apt-get install -y xvfb

      - name: Run ruff
        if: matrix.python-version == '3.12'
        run: ruff check .

      - name: Dead code check
        if: matrix.python-version == '3.12'
        continue-on-error: true
        run: |
          pip install vulture
          vulture gwexpy/ --min-confidence 80

      - name: Run tests (without GUI)
        if: "!contains(github.event.head_commit.message, '[gui-test]')"
        env:
          PYTHONFAULTHANDLER: "1"
        run: |
          pytest -q --forked --nbmake --nbmake-timeout=600 --nbmake-kernel=python3 --cov=gwexpy --ignore=tests/gui/ tests/ gwexpy/

      - name: Run paper notebooks
        if: "!contains(github.event.head_commit.message, '[gui-test]')"
        env:
          PYTHONFAULTHANDLER: "1"
        run: |
          if [ -d notebooks/ ] && find notebooks/ -name '*.ipynb' -print -quit | grep -q .; then
            pytest -q --nbmake notebooks/ --nbmake-timeout=600 --nbmake-kernel=python3
          else
            echo "No notebooks found in notebooks/, skipping."
          fi

      - name: Run tests (with GUI)
        if: contains(github.event.head_commit.message, '[gui-test]')
        env:
          PYTHONFAULTHANDLER: "1"
          PYTEST_SCREENSHOT_DIR: artifacts/screenshots
        run: |
          xvfb-run -a -s "-screen 0 1920x1080x24" pytest -q --forked --nbmake --nbmake-timeout=600 --nbmake-kernel=python3 --cov=gwexpy tests/ gwexpy/

      - name: Run paper notebooks (with GUI)
        if: contains(github.event.head_commit.message, '[gui-test]')
        env:
          PYTHONFAULTHANDLER: "1"
        run: |
          if [ -d notebooks/ ] && find notebooks/ -name '*.ipynb' -print -quit | grep -q .; then
            xvfb-run -a -s "-screen 0 1920x1080x24" pytest -q --nbmake notebooks/ --nbmake-timeout=600 --nbmake-kernel=python3
          else
            echo "No notebooks found in notebooks/, skipping."
          fi

      - name: Upload GUI failure screenshots
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: gui-screenshots
          path: artifacts/screenshots
          if-no-files-found: ignore

  test-conda-full:
    runs-on: ubuntu-latest
    continue-on-error: true

    steps:
      - uses: actions/checkout@v4

      - name: Set up micromamba (conda-forge)
        uses: mamba-org/setup-micromamba@v2
        with:
          environment-name: gwexpy-full
          cache-environment: true
          cache-downloads: true
          # Use condarc (supported input) to enforce channel settings.
          condarc: |
            channels:
              - conda-forge
            channel_priority: strict
          init-shell: bash
          generate-run-shell: false
          post-cleanup: none
          create-args: >-
            python=3.11
            pip

      - name: Install system dependencies (conditional)
        if: contains(github.event.head_commit.message, '[gui-test]')
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb

      - name: Install dependencies (minimal, stable)
        shell: bash -l {0}
        run: |
          micromamba run -n gwexpy-full python -m pip install --upgrade pip
          # Install only dependencies required to run test workflows.
          micromamba run -n gwexpy-full pip install "numpy<2.0" "scipy<1.14" "astropy<7.0" pandas matplotlib h5py "gwpy>=4.0.0,<5.0.0"
          micromamba run -n gwexpy-full pip install scikit-learn
          micromamba run -n gwexpy-full pip install -e . --no-deps
          micromamba run -n gwexpy-full pip install pytest pytest-cov nbmake pytest-forked

      - name: Install GUI dependencies (conditional)
        if: contains(github.event.head_commit.message, '[gui-test]')
        shell: bash -l {0}
        run: |
          micromamba run -n gwexpy-full pip install pytest-qt qtpy PyQt5 pyqtgraph
          # Optional tools: don't fail this job if unavailable.
          micromamba run -n gwexpy-full pip install cython || true
          if micromamba run -n gwexpy-full python scripts/install_minepy.py; then
            micromamba run -n gwexpy-full pip install mictools || true
          else
            echo "Skipping optional mictools setup (minepy build failed)."
          fi

      - name: Run unit tests (forked, without GUI)
        if: "!contains(github.event.head_commit.message, '[gui-test]')"
        timeout-minutes: 30
        env:
          PYTHONFAULTHANDLER: "1"
        shell: bash -l {0}
        run: |
          micromamba run -n gwexpy-full pytest -q --forked --cov=gwexpy --ignore=tests/gui/ tests/

      - name: Run unit tests (forked, with GUI)
        if: contains(github.event.head_commit.message, '[gui-test]')
        timeout-minutes: 30
        env:
          PYTHONFAULTHANDLER: "1"
        shell: bash -l {0}
        run: |
          micromamba run -n gwexpy-full xvfb-run -a -s "-screen 0 1920x1080x24" \
            pytest -q --forked --cov=gwexpy tests/

      - name: Run notebook tests
        timeout-minutes: 30
        env:
          PYTHONFAULTHANDLER: "1"
        shell: bash -l {0}
        run: |
          if ! find gwexpy -name '*.ipynb' -print -quit | grep -q .; then
            echo "No notebooks under gwexpy/, skipping nbmake."
            exit 0
          fi
          micromamba run -n gwexpy-full xvfb-run -a -s "-screen 0 1920x1080x24" \
            pytest -q --nbmake --nbmake-timeout=600 --nbmake-kernel=python3 gwexpy/
