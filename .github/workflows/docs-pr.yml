name: Docs PR

on:
  pull_request:
    branches: [main]
    paths:
      - "CHANGELOG.md"
      - "docs/**"
      - "docs_redesign/**"
      - "scripts/check_terms.py"
      - "scripts/check_docs_sync.py"
      - "scripts/check_forbidden_artifacts.py"
      - "scripts/run_quickstart_test.py"
      - "scripts/notebook_gen/check_changed_notebooks.py"
      - "scripts/notebook_gen/prepare_docs_notebook_tree.py"
      - "docs/requirements.txt"
      - "environment.yml"
      - "pyproject.toml"
      - ".github/workflows/docs-pr.yml"

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  docs-pr:
    name: Docs build and smoke checks
    runs-on: ubuntu-latest
    timeout-minutes: 45
    defaults:
      run:
        shell: bash -leo pipefail {0}
    env:
      PYTHONFAULTHANDLER: "1"
      NBS_EXECUTE: never
      NBS_ALLOW_ERRORS: "0"
      MPLCONFIGDIR: /tmp/matplotlib
    steps:
      - name: Checkout repository
        timeout-minutes: 5
        uses: actions/checkout@v7
        with:
          # Full history is required for base/head changed-notebook detection.
          fetch-depth: 0

      - name: Set up micromamba
        uses: mamba-org/setup-micromamba@v3
        with:
          environment-file: environment.yml
          environment-name: gwexpy
          create-args: >-
            python=3.11
          init-shell: bash

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgl1 pandoc fonts-noto-cjk

      - name: Provision gwexpy docs environment
        run: |
          python -m pip install --upgrade pip
          # Match preview/production: execute the public notebooks with their
          # optional analysis, control, and I/O dependencies available.
          python -m pip install -e ".[all]"
          python -m pip install -r docs/requirements.txt
          python -m pip install sphinx-intl papermill nbformat nbclient nbconvert

      - name: Check terminology consistency
        run: python scripts/check_terms.py

      - name: Check JA/EN synchronization
        run: python scripts/check_docs_sync.py

      - name: Check forbidden generated artifacts
        run: python scripts/check_forbidden_artifacts.py

      - name: Run quickstart tests
        run: |
          python scripts/run_quickstart_test.py --lang ja
          python scripts/run_quickstart_test.py --lang en

      - name: Check changed notebooks
        run: |
          python scripts/notebook_gen/check_changed_notebooks.py \
            --base "${{ github.event.pull_request.base.sha }}" \
            --head "${{ github.event.pull_request.head.sha }}"

      - name: Prepare temporary docs source tree
        id: prepare_docs_tree
        run: |
          docs_src="${RUNNER_TEMP}/gwexpy-docs-pr-src"
          python scripts/notebook_gen/prepare_docs_notebook_tree.py \
            --output-root "${docs_src}" \
            --mode changed \
            --base "${{ github.event.pull_request.base.sha }}" \
            --head "${{ github.event.pull_request.head.sha }}" \
            --jobs 4
          echo "docs_src=${docs_src}" >> "${GITHUB_OUTPUT}"

      - name: Build Sphinx HTML
        run: |
          docs_src="${{ steps.prepare_docs_tree.outputs.docs_src }}"
          sphinx-build -b html -W --keep-going \
            -D nbsphinx_execute=never \
            -D nbsphinx_allow_errors=0 \
            "${docs_src}/docs" "${RUNNER_TEMP}/gwexpy-docs-pr-html"

  docs-redesign-pr:
    name: docs_redesign build smoke check
    runs-on: ubuntu-latest
    timeout-minutes: 60
    defaults:
      run:
        shell: bash -leo pipefail {0}
    env:
      PYTHONFAULTHANDLER: "1"
      MPLCONFIGDIR: /tmp/matplotlib
      OPENBLAS_NUM_THREADS: "1"
      OMP_NUM_THREADS: "1"
      MKL_NUM_THREADS: "1"
      NUMEXPR_NUM_THREADS: "1"
    steps:
      - name: Checkout repository
        timeout-minutes: 5
        uses: actions/checkout@v7

      - name: Set up micromamba
        uses: mamba-org/setup-micromamba@v3
        with:
          environment-file: environment.yml
          environment-name: gwexpy
          create-args: >-
            python=3.11
          init-shell: bash

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y pandoc fonts-noto-cjk

      - name: Provision docs_redesign build environment
        run: |
          python -m pip install --upgrade pip
          # Match preview/production: execute the public notebooks with their
          # optional analysis, control, and I/O dependencies available.
          python -m pip install -e ".[all]"
          python -m pip install -r docs_redesign/requirements.txt

      # Build from a disposable copy so executed outputs are publish artifacts,
      # never changes to the clean notebooks committed in this repository.
      - name: Prepare isolated docs_redesign source
        id: prepare_docs_redesign
        run: |
          docs_root="${RUNNER_TEMP}/gwexpy-docs-redesign-pr"
          docs_src="${docs_root}/docs_redesign"
          mkdir -p "${docs_root}"
          rsync -a --delete --exclude "_build/" docs_redesign/ "${docs_src}/"
          cp CHANGELOG.md "${docs_root}/CHANGELOG.md"
          echo "docs_src=${docs_src}" >> "${GITHUB_OUTPUT}"

      # Same plain `sphinx-build -b html` invocation (no -W) that
      # docs-redesign-preview.yml and docs-pages.yml use for the real
      # deploy, so a green build here is the same build that would run at
      # deploy time -- it catches broken toctrees/links (build errors),
      # not the pre-existing warning baseline (31 EN / 69 JA, mostly
      # offline intersphinx timeouts) which this repo does not yet treat
      # as a hard gate for this tree.
      - name: Build EN HTML
        run: |
          docs_src="${{ steps.prepare_docs_redesign.outputs.docs_src }}"
          sphinx-build -b html -d "${docs_src}/_build/doctrees_en" \
            "${docs_src}" "${docs_src}/_build/html"

      - name: Build JA HTML
        run: |
          docs_src="${{ steps.prepare_docs_redesign.outputs.docs_src }}"
          sphinx-build -b html -D language=ja -d "${docs_src}/_build/doctrees_ja" \
            "${docs_src}" "${docs_src}/_build/html/ja"
