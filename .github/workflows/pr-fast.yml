name: PR Fast

on:
  pull_request:
    branches: [main]
    paths-ignore:
      - "docs/**"
      - "**/*.md"
      - ".github/ISSUE_TEMPLATE/**"
      - ".github/PULL_REQUEST_TEMPLATE.md"
      - ".github/pull_request_template.md"
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  validate:
    name: Ruff, mypy, pytest, and smoke build
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -leo pipefail {0}
    env:
      PYTHONFAULTHANDLER: "1"
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up micromamba
        uses: mamba-org/setup-micromamba@v3
        with:
          environment-file: environment.yml
          environment-name: gwexpy
          create-args: >-
            python=3.11
          init-shell: bash

      - name: Provision gwexpy environment
        run: |
          python -m pip install --upgrade pip build
          python -m pip install -e . --no-deps
          python -m pip install PyQt5
          python -m pip install iminuit

      - name: Generate synthetic test fixtures
        run: python tests/fixtures/generate_fixtures.py

      - name: Run Ruff
        run: ruff check gwexpy tests

      - name: Check forbidden generated artifacts
        run: python scripts/check_forbidden_artifacts.py

      - name: Run mypy
        run: mypy gwexpy tests/docs/test_tutorial_notebook_quality.py --ignore-missing-imports

      - name: Run pytest
        run: |
          pytest -q -m "not network and not nds" \
            --ignore=tests/docs/test_docs_notebooks.py \
            --ignore=tests/gui/ \
            --ignore=tests/nds/ \
            --ignore=tests/io/ \
            --ignore=tests/segments/ \
            --ignore=tests/table/ \
            --ignore=tests/test_geomap.py \
            --ignore=tests/time/test_time.py \
            --ignore=tests/test_fitting_highlevel.py \
            --ignore=tests/timeseries/test_matrix_analysis.py \
            --ignore=tests/types/test_series_matrix_io.py \
            tests/

  io-contract-gate:
    name: Core I/O contract gate
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -leo pipefail {0}
    env:
      PYTHONFAULTHANDLER: "1"
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up micromamba
        uses: mamba-org/setup-micromamba@v3
        with:
          environment-file: environment.yml
          environment-name: gwexpy
          create-args: >-
            python=3.11
          init-shell: bash

      - name: Provision gwexpy environment
        run: |
          python -m pip install --upgrade pip build
          python -m pip install -e . --no-deps
          python -m pip install PyQt5
          python -m pip install iminuit

      - name: Generate synthetic test fixtures
        run: python tests/fixtures/generate_fixtures.py

      - name: Run I/O contract tests
        run: |
          pytest -q -m "not network and not nds" \
            tests/io/test_io_contract.py \
            tests/io/test_io_docs_contract_sync.py \
            tests/io/ \
            tests/segments/ \
            tests/table/

      - name: Build wheel
        run: python -m build --wheel --no-isolation

      - name: Import smoke test
        run: |
          python - <<'PY'
          import gwexpy

          print(f"gwexpy version: {gwexpy.__version__}")
          PY

  io-optional-gate:
    name: Optional dependency gate
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -leo pipefail {0}
    env:
      PYTHONFAULTHANDLER: "1"
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up micromamba
        uses: mamba-org/setup-micromamba@v3
        with:
          environment-file: environment.yml
          environment-name: gwexpy
          create-args: >-
            python=3.11
          init-shell: bash

      - name: Provision gwexpy environment
        run: |
          python -m pip install --upgrade pip build
          python -m pip install -e . --no-deps
          python -m pip install freezegun xarray netCDF4 nptdms pydub tinytag

      - name: Run optional dependency tests
        run: |
          pytest -q \
            tests/io/test_optional_deps.py \
            tests/io/test_netcdf4_reader.py \
            tests/io/test_tdms_reader.py \
            tests/io/test_audio_metadata.py

  interop-contract-gate:
    name: Interop contract gate
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -leo pipefail {0}
    env:
      PYTHONFAULTHANDLER: "1"
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up micromamba
        uses: mamba-org/setup-micromamba@v3
        with:
          environment-file: environment.yml
          environment-name: gwexpy
          create-args: >-
            python=3.11
          init-shell: bash

      - name: Provision gwexpy environment
        run: |
          python -m pip install --upgrade pip build
          python -m pip install -e . --no-deps

      - name: Run interop contract tests
        run: |
          pytest -q \
            tests/interop/test_interop_contract.py \
            tests/interop/test_interop_docs_contract_sync.py \
            tests/interop/test_mt_mock.py
