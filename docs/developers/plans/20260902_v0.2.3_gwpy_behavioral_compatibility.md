# v0.2.3 GWpy 既定挙動互換性の安定化

Status: in-progress

## Goal

この計画は、GWpy 4.0.1 と 4.0.2 が正常に観測可能な既定挙動を、GWexpy v0.2.3 の既定経路で安定させるための patch-only release 計画である。

対象読者は、GWpy 対応 API、I/O、CI qualification を変更または審査する開発者である。

GWpy が有限な値を返して正常終了する既存 API では、GWexpy も値、shape、選択 sample、軸、成功または例外の種別を一致させる。

例外の比較は成功または失敗と exception class を対象とし、例外 message は public contract または既存 upstream test が要求するときだけ比較する。

NaN と Inf を含む配列は、値の直接比較ではなく mask と有限値部分を比較する。

本 release は presentation-only の plot 差異を対象外とし、新しい public API、依存関係、データ format を追加しない。

Issue #634 の CSV 全時刻 scale、#688 の full rational axis、SeriesMatrix、GUI、性能機能の作業はこの release から除外する。

## Architecture

GWexpy の既定 API は GWpy 互換の親実装を最初の経路とする。

GWexpy 固有機能は、GWpy が扱わない入力または明示的な既存 helper に限定して別経路に入れる。

native HDF5 および CSV の標準経路は、GWpy oracle が成功する場合に同じ論理 topology と可視結果を持つ。

private exact-time sidecar は後方互換性のために保持できるが、GWpy から可視な group、path、native attribute、payload topology を変えてはならない。

互換性判断は、実装 branch 上の GWexpy と、別々に導入した GWpy 4.0.1 および 4.0.2 oracle の differential test で記録する。

## Tech Stack

- Python と Astropy units。
- GWexpy の既存 test suite、pytest、ruff、mypy、Sphinx。
- 隔離した GWpy 4.0.1 と 4.0.2 環境。
- HDF5、CSV、matplotlib の既存依存関係。
- GitHub Actions の既存 19 qualification cell と `gwexpy-release-contracts-v1`。

## 固定された識別子と作業境界

| 項目 | 固定値または判断 |
| --- | --- |
| 実装 base | `a8085b71446d3ef3417a7e5b5ac8efb156368eac` |
| 公開済み anchor | `2503743cf654606a5baa83c7b7e7c8b8e1e06596` |
| 作業 branch | `fix/v023-gwpy-behavioral-compat` |
| oracle | 個別に導入した GWpy 4.0.1 と 4.0.2 |
| release 種別 | v0.2.3 patch-only behavioral compatibility stabilization |
| 全体状態 | `in-progress` |

この worktree は上記の実装 base に基づく。

`latest main` を基準にしたり、`origin/main` を rebase または re-resolve したりしない。

base を変えると既存の differential evidence が無効になるため、base を変える場合は計画と manifest を改訂し、証拠を採り直す。

## 目標と非目標

1. GWpy 既定 API の成功、値、metadata、例外 class を differential evidence で閉じる。
2. #639 の source/MRO 起点の public override inventory を release 前に terminal state へ閉じる。
3. HDF5 append、constructor、時刻 API、FFT、spectral API、plot 引数、CSV、collection の既定互換性を修正または no-finding として証拠化する。
4. v0.2.2 と v0.2.3 だけを許可する release tooling を、既存 19 cell と publish dependency を保って導入する。

次の変更は行わない。

- 新規 public 引数、新規依存関係、新規 interchange format。
- GWpy が失敗する colon または slash の HDF5 名を成功させる互換性要件。
- pixel、style、layout 表現を含む plot の視覚的同一性判定。
- #634、#688、SeriesMatrix、GUI、性能改善の feature work。

## 公開挙動の決定

### #639 の完全閉包

Phase 0 で、GWexpy source と MRO から、GWpy に対応物がある public override を constructor を含めて機械抽出し、手動 review で補正した inventory を作る。

inventory の一行は API、定義 source、MRO 上の GWpy counterpart、GWexpy override、GWpy version、fixture、GWpy/GWexpy observation、test または evidence、issue、terminal state を記録する。

terminal state は `fixed`、`no-finding`、`GWpy-fails`、`GWexpy-only` の四つだけである。

作業中だけは `unreviewed` と `differential-required` を使えるが、release candidate の inventory では件数を必ず 0 にする。

確認済みの patch defect を deferred にしない。

default がすでに compatible である場合だけ、minor follow-up を本 release 後の別 issue にできる。

### HDF5 append の native 契約

Phase 0 の最初の red test は、`TimeSeriesDict` HDF5 append で既存 data が失われる事例とする。

native/default の read/write/append/overwrite は、oracle と names、group、append、overwrite、logical keys、topology、成功または例外 class を比較する。

oracle が colon または slash を含む name で失敗する場合、GWexpy が成功する必要はない。

exact sidecar は private augmentation とし、GWpy-visible topology、path、native attribute を変更しない。

旧 manifest は読み続けられなければならない。

custom append は既存 payload を truncate してはならない。

### constructor の routing

親例外を routing または retry の判定に使わない。

GWpy が support する input class と calling form は変更せず一回だけ parent に渡す。

GWexpy-only の time-like input は、parent 呼出前の明示的な type predicate と routing predicate で normalizer に送る。

parent が処理した input は retry しない。

positional epoch と keyword epoch は同じ classifier を通す。

`t0_ns` は独立した exact authority のままとし、矛盾する指定は拒否する。

`TimeSeries(x, None, 1000, 1 * u.ms)` と、同じ値を keyword で渡す form は等価であることを test する。

### 時刻 API

Issue #709 の `to_gps`、#710 の date component、#711 の `tconvert` gps-or-date を GWpy default と比較する。

3 から 7 要素の numeric tuple/list で date-shaped な input は GWpy に送り、`(2017, 13, 1)` のような不正 date は vector fallback せず GWpy の失敗を保つ。

明示的 vector input は GWexpy extension として維持する。

Phase 3 と Phase 4 では `from_gps` も、GWpy 成功 vector、numeric string、LIGOTimeGPS-like input、fractional GPS、leap-second 近傍で differential evidence を採る。

観測可能な差異がなければ `no-finding` を terminal state にする。

### `FrequencySeries.ifft`

`FrequencySeries.ifft` は parent が返す `t0` と `dt` を保つ。

absolute time から cadence を再構成しない。

4 Hz、4096 Hz、1 MHz、10 MHz、odd/even length、large GPS epoch の fixture を比較する。

### spectral API

Issue #698 の CSD default unit は parent の unit を保持する。

Issue #699 の default Rayleigh は GWpy と一致させ、補正 helper を public API にしない。

Issue #702 の steady path の `epsilon=None` は GWpy と一致させ、transient と explicit `epsilon` は既存 extension とする。

Issue #703 は API ごとに differential test を採り、wholesale な guard removal をしない。

### plot の positional layout

次の positional layout を keyword 呼出と同じ binding にする。

| API | positional layout | 成功 fixture |
| --- | --- | --- |
| `TimeSeries.plot` | `method, figsize, xscale` | `ts.plot('plot', (10, 4), 'linear')` |
| `FrequencySeries.plot` | `method, xscale` | `fs.plot('plot', 'linear')` |
| `Spectrogram.plot` | `method, figsize, xscale` | `spec.plot('imshow', (10, 6), 'linear')` |
| `TimeSeriesDict.plot` | `label, method, figsize, xscale` | `tsd.plot('name', 'plot', (10, 4), 'linear')` |

duplicate argument と excess positional argument は、GWpy と成功または失敗、exception class を比較する。

`step` は GWpy が失敗する場合があるため、GWpy 成功 fixture として扱わない。

pixel または style の比較は行わない。

### CSV routing

次の route table を既定契約とする。

| 操作 | 条件 | 経路 |
| --- | --- | --- |
| TimeSeries read | plain `.csv` または `format='csv'`、かつ `config`、`channels`、`timezone`、`resample`、`resample_method` が明示されない | GWpy native |
| TimeSeries read | enhanced-only key のいずれかが明示される | 既存 enhanced reader |
| direct CSV helper | 常に | 既存 enhanced reader |
| TimeSeries write | 標準 `TimeSeries.write(..., format='csv')` | GWpy native に args/kwargs を forward |
| enhanced CSV write | direct existing helper | 既存 enhanced writer |
| FrequencySeries ordinary CSV | 標準呼出 | GWpy native |

complex CSV は write outcome、出力、exception class を比較する。

roundtrip は GWpy 自身が成功する fixture だけで比較する。

### collection API

collection `crop` は self を mutate し、同じ self を返す。

`prepend` は key-wise に動作する。

`filterba` shim を作る前に、GWpy 3.x の historical signature と defaults を調査して記録する。

## Phase roadmap

### Phase 0: inventory と HDF5 data-loss を閉じる

Status: in-progress

1. #639 inventory の抽出条件、source/MRO 実行ログ、review 結果、terminal state schema を manifest に固定する。
2. public constructor を含む全 counterpart override を列挙し、`unreviewed` と `differential-required` を release 前に 0 にする担当と evidence path を割り当てる。
3. `TimeSeriesDict` HDF5 append data-loss の red test を最初に追加し、GWpy 4.0.1 と 4.0.2 に対する native/default baseline を取得する。
4. names、group、append、overwrite、logical keys、topology、成功または exception class を比較し、custom append が truncate しない実装へ限定して修正する。
5. private exact sidecar と旧 manifest readback の回帰 test を追加する。
6. colon/slash name の結果は oracle が失敗するなら `GWpy-fails` と記録し、成功化を目的にしない。

Phase 0 の exit criterion は、red test が原因を再現し、修正候補が native topology を変えず、inventory の初回完全列挙が review 可能であることとする。

### Phase 1: constructor と collection

Status: planned

1. parent exception に依存する fallback/retry を除き、explicit predicate による pre-parent routing を実装する。
2. GWpy-supported input class と positional/keyword calling form が一回だけ parent を通ることを test する。
3. `t0_ns` authority と conflict rejection を regression test する。
4. positional と keyword の `TimeSeries` epoch form の等価性を test する。
5. collection crop の mutation/identity と key-wise prepend を GWpy と比較する。
6. `filterba` は GWpy 3.x historical evidence を先に記録し、その signature/default と矛盾しない shim だけを許す。

### Phase 2: 時刻変換と FFT metadata

Status: planned

1. #709、#710、#711 の input class、戻り値、failure class を GWpy 4.0.1/4.0.2 と比較する。
2. date-shaped 3-7 numeric tuple/list を GWpy に委譲し、不正 date が vector fallback しないことを test する。
3. explicit vector extension を既存 public behavior の範囲で保存する。
4. `FrequencySeries.ifft` の `t0`、`dt`、shape、値を sample rate、length parity、large GPS epoch ごとに比較する。
5. absolute-time cadence reconstruction を導入しないことを code review checklist に入れる。

### Phase 3: spectral API と `from_gps` differential

Status: planned

1. #698 CSD、#699 Rayleigh、#702 epsilon、#703 各 API の fixture と値/unit/axis/exception evidence を記録する。
2. corrected helper は private のままとし、新しい public export を追加しない。
3. steady `epsilon=None`、transient、explicit epsilon の経路を分けて test する。
4. `from_gps` の success vector、numeric string、LIGOTimeGPS-like input、fractional GPS、leap-second 近傍を二つの GWpy oracle と比較する。
5. 観測差異がない `from_gps` entry は `no-finding` として inventory を閉じる。

### Phase 4: plot と CSV の既定経路

Status: planned

1. 四つの plot API の positional/keyword binding、duplicate、excess positional の success/failure class を differential test する。
2. `step` を success fixture と仮定せず、oracle 結果を `GWpy-fails` または compatible result として記録する。
3. CSV route table の各条件を parameterized test にし、明示されない enhanced-only key と明示的 `None` を区別して評価する。
4. native writer の args/kwargs forwarding と enhanced direct helper の境界を test する。
5. ordinary FrequencySeries CSV と complex CSV の outcome/output/exception evidence を記録する。

### Phase 5: release tooling と証拠固定

Status: planned

1. `gwexpy-release-contracts-v1` を保持し、`qualification_profile` は追加しない。
2. v0.2.3 entry を追加する。
3. `qualify` と `qualification_evidence` の allowlist は厳密に `{0.2.2,0.2.3}` とする。
4. 同じ 19 cell を維持する。
5. v0.2.2/v0.2.3 の schema/prefix switch を明示し、未知 version は fail closed にする。
6. `publish.needs` を保ち、`always` と `continue-on-error` を追加しない。
7. candidate evidence は実行された job を確認し、承認済 expected-skip baseline と observed skip set を記録する。

## Testing and release gates

各 compatibility fixture は GWpy version、input、GWpy observation、GWexpy observation、値/metadata comparator、test path、issue、inventory state を evidence に記録する。

required compatibility/qualification test に新しい skip が一つでも発生した場合、または approved expected-skip baseline にない skip が一つでもある場合、それを **unexpected skip** と定義する。

既にその baseline に記録済みの optional-dependency skip は許容する。

candidate evidence は baseline の識別子と observed skip set を明記し、required tests について `observed - baseline = empty` を示さなければならない。

release candidate には `unreviewed`、`differential-required`、unexpected failure、unexpected skip がいずれも 0 であることを要求する。

local gate は次の順で実行する。

1. Phase ごとの GWpy 4.0.1/4.0.2 focused differential tests。
2. HDF5 append、constructor、time、FFT、spectral、plot、CSV、collection focused tests。
3. `conda run -n gwexpy pytest tests -q`。
4. `conda run -n gwexpy ruff check gwexpy tests`。
5. `conda run -n gwexpy mypy gwexpy`。
6. `conda run -n gwexpy ruff format --check gwexpy tests`。
7. strict documentation build、`git diff --check`、package build、`twine check --strict`。

CI gate は既存の 19 cell を実際に実行し、各 job の実行結果、同一 artifact digest、skip baseline 差分、qualification evidence を candidate SHA に結び付ける。

外部 CI、人間 review、tag、PyPI publication は本 worktree の実装権限外であり、candidate が全 gate を満たしてから別工程で実施する。

## 完了条件

- #639 inventory が source/MRO 起点で全 public counterpart override と constructor を含み、terminal state 以外が 0 である。
- confirmed patch defect が deferred されていない。
- HDF5 append、constructor、time API、FFT、spectral、plot、CSV、collection の判定が test と oracle evidence に結び付く。
- public API、依存関係、format を増やしていない。
- release tooling が v0.2.2/v0.2.3 allowlist、19 cell、fail-closed switch、publish dependency を保つ。
- expected-skip baseline に対する observed required skip 差分が空である。
- candidate evidence が実際に実行された CI jobs を示す。

## 実施体制、skill、見積もり

| 区分 | 割当 |
| --- | --- |
| coordinator | `gpt-5.6-sol`、effort `high` |
| independent review | `gpt-5.6-terra`、effort `high` |
| quota | High |
| required skills | `audit_api_elements`、`extend_gwpy`、`audit_io_backends`、`verify_physics`、`gwexpy_conda_jobs`、`prep_release`、`finalize_work` full |
| best core estimate | 3-5 h |
| known fixes/tests/docs/release tooling | 6-10 h |
| #639 exhaustive closure | 4-8 h |
| active release-ready work | 10-18 h |
| external CI/human/publication | 2-6 h+ |

`verify_physics` は時刻軸、unit、FFT metadata に物理的または数値的な判断が発生した場合に人間 review へ flag する。

## Appendix: review corrections retained

本文は以下の review correction を反映した corrected plan である。

### Initial Conditional GO corrections

- v0.2.3 を patch-only GWpy default behavioral compatibility stabilization と固定した。
- implementation base を固定し、main の追随、rebase、evidence の使い回しを禁止した。
- #639 を source/MRO の全 public override と constructor を含む release-blocking inventory とした。
- HDF5 append data loss を最初の red test とし、sidecar を private augmentation に限定した。
- release tooling の allowlist、19 cell、fail-closed、publish dependency を明文化した。

### Plot example correction

- `TimeSeries`、`FrequencySeries` の `step` を GWpy success fixture としない。
- success fixture は `ts.plot('plot', (10, 4), 'linear')`、`fs.plot('plot', 'linear')`、`spec.plot('imshow', (10, 6), 'linear')`、`tsd.plot('name', 'plot', (10, 4), 'linear')` とする。
- `step` は GWpy が失敗する場合に、成功を要求せず exception class comparison とする。

### Final GO hardenings

- constructor は parent exception から fallback を選ばず、pre-parent predicate で routing する。
- `from_gps` は success vector、numeric string、LIGOTimeGPS-like、fractional GPS、leap-second 近傍まで differential evidence を採る。
- unexpected skip は新規 skip または approved expected-skip baseline にない skip と厳密に定義し、candidate で差分空集合を確認する。
