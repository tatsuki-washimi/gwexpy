issue: 294
date: 2026-05-07
agent: Codex
skills:
  - doc-architect
scope:
  summary: "Update conda-forge roadmap status after staged-recipes PR creation and green CI."
  runtime_code_changed: false
  physics_behavior_changed: false
  public_install_docs_changed: false
  release_published: false
external_state:
  staged_recipes_pr:
    url: "https://github.com/conda-forge/staged-recipes/pull/33169"
    title: "Add gwexpy"
    state: open
    mergeable: true
    head_commit: "487d1cd"
    source: "currently opened from gwexpy==0.1.1; must be refreshed after the gwexpy==0.1.2 PyPI sdist is published"
    sha256: "pending after gwexpy==0.1.2 publication"
  ci:
    conda_forge_linter: success
    staged_recipes_linter: success
    azure_linux_64: success
    azure_osx_64: success
    azure_win_64: success
  feedstock:
    repo: "conda-forge/gwexpy-feedstock"
    exists: false
commands:
  - cmd: "rtk gh pr view 33169 --repo conda-forge/staged-recipes --json url,state,mergedAt,mergeable,reviewDecision,statusCheckRollup,comments,reviews"
    result: "Confirmed the staged-recipes PR is open, mergeable, comment-only from the linter, and CI-green."
  - cmd: "rtk gh repo view conda-forge/gwexpy-feedstock --json name,url,description,defaultBranchRef"
    result: "Repository was not found, confirming the feedstock has not been created yet."
  - cmd: "rtk gh issue comment 294 --repo tatsuki-washimi/gwexpy --body-file /tmp/gwexpy-issue-294-20260430-status.md"
    result: "Recorded the 2026-04-30 status on issue #294."
changed_files:
  - path: docs/developers/plans/active/2026-04-28-conda-forge-roadmap.md
    change: "Adds current staged-recipes PR status, updates the recipe model to the CI-tested v1 form, and separates remaining review/feedstock/install gates."
  - path: docs/developers/plans/active/2026-04-27-issue-burn-down-roadmap.md
    change: "Updates Wave 5 status now that PyPI 0.1.2 is the documentation baseline and staged-recipes PR #33169 must be refreshed from 0.1.1."
  - path: tests/test_conda_forge_roadmap.py
    change: "Updates drift guards for astropy-base, build.python.entry_points, and the CI-tested smoke-test coverage."
  - path: docs/developers/plans/manifests/audit-manifest-294-conda-forge-staged-recipes-status.yaml
    change: "Records this documentation-only status update."
validation:
  completed:
    - cmd: "rtk pytest -q tests/test_conda_forge_roadmap.py -p no:cacheprovider"
      result: "5 passed"
    - cmd: "rtk ruff check tests/test_conda_forge_roadmap.py"
      result: "No issues found"
    - cmd: "rtk ruff format --check tests/test_conda_forge_roadmap.py"
      result: "1 file already formatted"
    - cmd: "rtk python -c \"import pathlib, yaml; yaml.safe_load(pathlib.Path('docs/developers/plans/manifests/audit-manifest-294-conda-forge-staged-recipes-status.yaml').read_text())\""
      result: "parsed successfully"
    - cmd: "rtk git diff --check"
      result: "clean"
human_review:
  required: false
  reason: "Documentation and roadmap guard update only; no runtime, physics, statistical, or public install behavior changed."
remaining:
  - "After gwexpy==0.1.2 is published and smoke-tested, refresh staged-recipes PR #33169 from gwexpy==0.1.1 to the gwexpy==0.1.2 PyPI sdist, record the 0.1.2 SHA256 hash, and rerun local plus remote validation."
  - "Wait for conda-forge reviewer/maintainer review and merge of staged-recipes PR #33169."
  - "Confirm feedstock creation after merge."
  - "Run a fresh conda install smoke test only after the package appears on the conda-forge channel."
  - "Update public install docs after the conda-forge package is live and smoke-tested."
