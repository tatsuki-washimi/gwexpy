---
# Audit manifest for deferring the experimental GUI app from the first PyPI surface
# Generated by Codex
# Date: 2026-04-29

issue: 274
branch: codex/defer-gui-from-first-pypi
agent_role: Release/docs coordination
agent_model: Codex session default

skills:
  - release-coordinator
  - doc-architect

docs_and_tests_only: false
runtime_modules_changed: false
packaging_metadata_changed: true

decision:
  status: accepted_by_maintainer
  summary: >
    The GUI app is independent from GWexpy's role as a Python analysis library,
    so it is deferred from the first PyPI supported surface. The core Python
    library release can proceed without treating #274 as release-blocking.
  policy:
    - "Do not install a gwexpy.gui console script in the first PyPI release."
    - "Keep GUI dependencies outside gwexpy[all]."
    - "Track GUI payload metadata and renderer work as post-release #274 stabilization."

commands_executed:
  - cmd: "Reviewed release-coordinator skill instructions"
    result: "Reviewed release coordination workflow."
  - cmd: "Reviewed doc-architect skill instructions"
    result: "Reviewed documentation synchronization workflow."
  - cmd: "rtk python -c \"import tomllib; data=tomllib.load(open('pyproject.toml','rb')); assert data['project']['scripts'] == {'gwexpy': 'gwexpy.cli:main'}\""
    result: "Confirmed pyproject exposes only the core gwexpy console script."
  - cmd: "rtk pytest -q tests/test_conda_forge_roadmap.py -p no:cacheprovider"
    result: "5 passed."
  - cmd: "rtk python scripts/check_release_metadata.py"
    result: "Metadata consistency check passed."
  - cmd: "rtk git diff --check"
    result: "No whitespace errors."
  - cmd: "rtk ruff check tests/test_conda_forge_roadmap.py"
    result: "No issues found."
  - cmd: "rtk ruff format --check tests/test_conda_forge_roadmap.py"
    result: "1 file already formatted."
  - cmd: "rtk python -m build --outdir /tmp/gwexpy-dist-gui-defer"
    result: "Built wheel and sdist after network access was approved for isolated build dependencies."
  - cmd: "rtk python - <<'PY' ... inspect wheel entry_points.txt ... PY"
    result: "Confirmed wheel contains only gwexpy = gwexpy.cli:main and no gwexpy.gui console script."
  - cmd: "rtk python scripts/check_release_artifacts.py /tmp/gwexpy-dist-gui-defer"
    result: "Release artifacts passed hygiene checks."
  - cmd: "Reviewer agent review of PR #349"
    result: "Found one medium documentation inconsistency: installation tables over-described gwexpy[all] even though GUI is excluded."
  - cmd: "PR #349 follow-up reviews"
    result: "Addressed stronger GUI-dependency coverage, source/development GUI launch wording, and manifest path hygiene."
  - cmd: "rtk rg -n \"non-GUI|GUI を除く|gwexpy\\[all\\]\" docs/web/en/user_guide/installation.md docs/web/ja/user_guide/installation.md"
    result: "Confirmed EN/JA install tables now describe gwexpy[all] as non-GUI optional features."
  - cmd: "rtk sphinx-build -b html -D nbsphinx_execute=never docs /tmp/gwexpy-docs-html-gui-defer"
    result: "Not run: sphinx-build executable is unavailable in this environment."
  - cmd: "rtk python -m sphinx -b html -D nbsphinx_execute=never docs /tmp/gwexpy-docs-html-gui-defer"
    result: "Failed because sphinx is not installed in this environment."

files_changed:
  - path: pyproject.toml
    change: "Removed the gwexpy.gui console script and documented GUI dependencies as source/development-only, outside the first PyPI supported surface."
  - path: CHANGELOG.md
    change: "Recorded the user-visible packaging decision in Unreleased."
  - path: docs/web/en/user_guide/installation.md
    change: "Marked the GUI extra as source/development-only and outside first PyPI support; clarified gwexpy[all] excludes GUI."
  - path: docs/web/ja/user_guide/installation.md
    change: "Marked the GUI extra as source/development-only and outside first PyPI support; clarified gwexpy[all] excludes GUI."
  - path: docs/web/en/user_guide/gui.md
    change: "Removed console-script launch instructions and documented the GUI as post-release/source-development track."
  - path: docs/web/ja/user_guide/gui.md
    change: "Removed console-script launch instructions and documented the GUI as post-release/source-development track."
  - path: docs/web/en/reference/api/gui.rst
    change: "Updated the GUI API warning and launch command."
  - path: docs/web/ja/reference/api/gui.rst
    change: "Updated the GUI API warning and launch command."
  - path: docs/developers/plans/active/2026-04-27-issue-burn-down-roadmap.md
    change: "Classified #274 as a post-release follow-up rather than release-blocking."
  - path: docs/developers/plans/active/2026-04-28-conda-forge-roadmap.md
    change: "Aligned conda-forge entry point guidance with the PyPI GUI deferral."
  - path: tests/test_conda_forge_roadmap.py
    change: "Added packaging metadata guards for the core console script and GUI dependency exclusion from gwexpy[all]."

test_results:
  pyproject_script_check: passed
  focused_conda_roadmap_tests: "5 passed"
  release_metadata_check: passed
  build_artifacts: passed
  wheel_entry_points_check: passed
  artifact_hygiene: passed
  diff_check: clean
  ruff_changed_test: clean
  ruff_format_changed_test: clean
  reviewer_agent: "one medium docs inconsistency found and addressed"
  pr_follow_up_reviews: "actionable items addressed"
  docs_build: not_run_sphinx_unavailable

risks:
  - "The gwexpy.gui Python package is still present in built artifacts for source compatibility, but the GUI app is not exposed as a first-release console script or supported PyPI surface."
  - "Full docs build should be rerun in CI or a docs-enabled environment."
