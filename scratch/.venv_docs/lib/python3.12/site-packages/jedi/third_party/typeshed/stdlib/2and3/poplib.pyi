import builtins
import socket
import ssl
import sys
from re import Pattern
from typing import Any, BinaryIO, Optional, overload

_LongResp = tuple[bytes, list[bytes], int]

class error_proto(Exception): ...

POP3_PORT: int
POP3_SSL_PORT: int
CR: bytes
LF: bytes
CRLF: bytes

class POP3:
    if sys.version_info >= (3, 0):
        encoding: str

    host: str
    port: int
    sock: socket.socket
    file: BinaryIO
    welcome: bytes
    def __init__(self, host: str, port: int = ..., timeout: float = ...) -> None: ...
    def getwelcome(self) -> bytes: ...
    def set_debuglevel(self, level: int) -> None: ...
    def user(self, user: str) -> bytes: ...
    def pass_(self, pswd: str) -> bytes: ...
    def stat(self) -> tuple[int, int]: ...
    def list(self, which: Optional[Any] = ...) -> _LongResp: ...
    def retr(self, which: Any) -> _LongResp: ...
    def dele(self, which: Any) -> bytes: ...
    def noop(self) -> bytes: ...
    def rset(self) -> bytes: ...
    def quit(self) -> bytes: ...
    def close(self) -> None: ...
    def rpop(self, user: str) -> bytes: ...
    timestamp: Pattern[str]

    if sys.version_info < (3, 0):
        def apop(self, user: str, secret: str) -> bytes: ...
    else:
        def apop(self, user: str, password: str) -> bytes: ...
    def top(self, which: Any, howmuch: int) -> _LongResp: ...
    @overload
    def uidl(self) -> _LongResp: ...
    @overload
    def uidl(self, which: Any) -> bytes: ...
    if sys.version_info >= (3, 5):
        def utf8(self) -> bytes: ...
    if sys.version_info >= (3, 4):
        def capa(self) -> dict[str, builtins.list[str]]: ...
        def stls(self, context: Optional[ssl.SSLContext] = ...) -> bytes: ...

class POP3_SSL(POP3):
    if sys.version_info >= (3, 0):
        def __init__(
            self,
            host: str,
            port: int = ...,
            keyfile: Optional[str] = ...,
            certfile: Optional[str] = ...,
            timeout: float = ...,
            context: Optional[ssl.SSLContext] = ...,
        ) -> None: ...
    else:
        def __init__(
            self, host: str, port: int = ..., keyfile: Optional[str] = ..., certfile: Optional[str] = ..., timeout: float = ...
        ) -> None: ...
    if sys.version_info >= (3, 4):
        # "context" is actually the last argument, but that breaks LSP and it doesn't really matter because all the arguments are ignored
        def stls(self, context: Any = ..., keyfile: Any = ..., certfile: Any = ...) -> bytes: ...
