from collections.abc import Callable, Container, Iterable
from re import Pattern
from typing import Any, Optional, Union

ALLOWED_TAGS: list[str]
ALLOWED_ATTRIBUTES: dict[str, list[str]]
ALLOWED_STYLES: list[str]
ALLOWED_PROTOCOLS: list[str]

INVISIBLE_CHARACTERS: str
INVISIBLE_CHARACTERS_RE: Pattern[str]
INVISIBLE_REPLACEMENT_CHAR: str

# A html5lib Filter class
_Filter = Any

class Cleaner:
    def __init__(
        self,
        tags: Container[str] = ...,
        attributes: Any = ...,
        styles: Container[str] = ...,
        protocols: Container[str] = ...,
        strip: bool = ...,
        strip_comments: bool = ...,
        filters: Optional[Iterable[_Filter]] = ...,
    ) -> None: ...
    def clean(self, text: str) -> str: ...

_AttributeFilter = Callable[[str, str, str], bool]
_AttributeDict = dict[str, Union[Container[str], _AttributeFilter]]

def attribute_filter_factory(attributes: Union[_AttributeFilter, _AttributeDict, Container[str]]) -> _AttributeFilter: ...

class BleachSanitizerFilter:  # TODO: derives from html5lib.sanitizer.Filter
    def __getattr__(self, item: str) -> Any: ...  # incomplete
