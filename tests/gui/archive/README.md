# GUI Archive

This directory is reserved for local, non-runtime GUI reference archives.

Binary snapshots such as `pyaggui.zip` are not used by the automated test
suite and are ignored by Git. If a reference archive becomes necessary for a
test, extract only the minimal fixture data into a documented fixture directory.
