# Agent Skills Index

## 利用可能なエージェントスキル一覧

### 1. 開発・実装
| スキル名 | 説明 |
| :--- | :--- |
| `add_type` | gwexpyに新しい配列型（Array/Series/Field）とコレクションを実装する |
| `extend_gwpy` | GWpy/Astropyクラスの安全な継承と拡張のためのガイドライン |
| `refactor_nb` | Jupyter Notebook (.ipynb) 内の import 文やコードセルを一括置換・リファクタリングする |
| `visualize_fields` | 多次元フィールドデータ（Field4D/Matrix等）の抽出ロジックと描画APIを、物理的整合性を保ちながら実装する |

### 2. 解析・リサーチ
| スキル名 | 説明 |
| :--- | :--- |
| `analyze_code` | 外部ライブラリや他言語で書かれたコードの実装を分析する |
| `compare_methods` | ライブラリ内の類似した信号処理手法の技術的・物理的な違いを分析し、比較解説付きのノートブックセクションを作成する |
| `profile` | 指定したコードの実行速度をプロファイリングし、ボトルネックを特定する |
| `calc_bode` | python-controlを使用して、メカニカルシステムの状態空間モデルから伝達関数（Bode Plot）を計算・表示する |

### 3. 品質保証・テスト
| スキル名 | 説明 |
| :--- | :--- |
| `lint` | RuffとMyPyを使用してコード品質をチェックする |
| `test_code` | pytestを使用してプロジェクトのテストスイートを実行する |
| `test_notebooks` | 全てのプロジェクトノートブックを順次実行し、エラーがないか確認する |
| `test_gui` | GUIアプリケーションの動作試験・デバッグを自動で行う |
| `review_repo` | リポジトリ全体を構造、コード品質、テスト、ドキュメントの観点から体系的にレビューし、優先度付きの改善レポートを生成する |
| `check_deps` | ソースコード内のimportとpyproject.tomlの依存関係の整合性をチェックする |
| `fix_notebook` | ノートブックのエラーを、そのノートブック自体の修正（セル内容・JSONスキーマ）またはソースコードの修正により解決する |
| `fix_mypy` | gwexpyのコードベースで頻発するMyPyエラーを効率的に解決するパターン集 |

### 4. ドキュメント
| スキル名 | 説明 |
| :--- | :--- |
| `build_docs` | Sphinxドキュメントをビルドする |
| `sync_docs` | ソースコードの実装に合わせてドキュメント (docstring, *.md) を更新する |
| `make_notebook` | 機能やテーマについて解説付きのJupyter Notebook (.ipynb) を生成する |

### 5. ワークフロー
| スキル名 | 説明 |
| :--- | :--- |
| `git_commit` | 作業終了後に不要な一時ファイルを削除し、gitにコミットする |
| `ignore` | 不要なファイル・ディレクトリを .gitignore に追加・管理する |
| `organize` | プロジェクトのディレクトリ構造を整理し、配置ミスのファイルをクリーンアップする |
| `prep_release` | バージョン更新、CHANGELOG整備、パッケージビルドなど、リリース前の準備を行う |
| `wrap_up` | 作業終了時の一連のフロー（検証、テスト、リント、ドキュメント更新、コミット）を一括実行する |
| `wrap_up_gwexpy` | gwexpy向けの一連の検証・整理・コミット手順をまとめて実行する |
| `estimate_effort` | 作業計画から必要な時間とLLMクオータ（トークン消費量）の見積もりを算出し、コストパフォーマンスを評価する |
| `setup_plan` | ユーザーの要求から具体的な作業計画を作成し、モデル・スキル・工数を提案して計画書を保存・更新する |
| `archive_plan` | 実装計画（implementation_plan.md）を日本語に翻訳し、モデル選定・リソース管理戦略を含めた正式な計画書として docs/developers/plans/ に保存する |
| `archive_work` | 作業完了後にタイムスタンプをつけて報告書を作成・保存し、必要に応じてagent skillsを追加・更新する |
| `conversation_report` | 会話全体の作業レポートをタイムスタンプ付きで保存する |

### 6. サイエンス
| スキル名 | 説明 |
| :--- | :--- |
| `check_physics` | 実装内容が数学的・物理学的に妥当か検証する |
| `debug_axes` | プロットのスケール（対数軸など）、目盛、表示範囲の不具合を診断し、意図した見た目に修正する |

### 7. メタ
| スキル名 | 説明 |
| :--- | :--- |
| `list_skills` | 登録されているスキル一覧をカテゴリー別に分類して表示する |
| `suggest_skill` | 現在の状況やタスクに合わせて、今使うべきおススメのスキルを提案する |
| `suggest_model` | 次のタスクの内容（コーディング、リサーチ、リファクタリングなど）に応じて、最適なLLMモデルを提案する |
| `learn_skill` | 会話履歴や作業内容から、再利用可能なエージェントスキルを生成または追記する |
| `recover_quota` | LLMのクオータ（利用制限）を管理・節約し、効率的に回復・継続するための戦略を実行する |
| `refactor_skills` | スキルの整理（統廃合・分離・分類の更新）を行い、エージェントの能力を洗練させる |

---

*このファイルは `list_skills` スキルによって自動生成されました。*